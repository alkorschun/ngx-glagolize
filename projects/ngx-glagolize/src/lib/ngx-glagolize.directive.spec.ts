import { NgxGlagolizeDirective } from './ngx-glagolize.directive';

describe('NgxGlagolizeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxGlagolizeDirective();
    expect(directive).toBeTruthy();
  });
});
